default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: cetaitmieuxavant_development

test:
  <<: *default
  database: cetaitmieuxavant_test

production:
  <<: *default
  database: cetaitmieuxavant_production
